{
  "name": "starfish-monorepo",
  "private": true,
  "scripts": {
    "build": "pnpm --filter @starfish/shared run build && pnpm -r --filter '@starfish/core' --filter '@starfish/memory' --filter '@starfish/chat' run build",
    "dev": "pnpm --filter './packages/*' dev"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "ts-node": "^10.9.2",
    "@types/node": "^20.11.20"
  }
}
